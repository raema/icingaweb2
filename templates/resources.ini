[icingaweb2]
type                = "db"
db                  = "mysql"
host                = "{{ db.host() }}"
port                = "3306"
dbname              = "{{ db.database() }}"
username            = "{{ db.user() }}"
password            = "{{ db.password() }}"


[icinga2]
type                = "db"
db                  = "mysql"
host                = "{{ db.host() }}"
port                = "3306"
dbname              = "{{ db.database() }}"
username            = "{{ db.user() }}"
password            = "{{ db.password() }}"
